### @7iomka/media

# Next.js Example

This example shows how to setup `@7iomka/media` for server-side rendering, via
[Next.js](https://nextjs.org/).

To run the example:

```
yarn install
yarn dev
```
